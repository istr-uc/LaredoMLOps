{{- if .Values.backend.deploy_postgres }}
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: {{ .Values.frontend.namespace}}
type: Opaque
data:
  POSTGRES_PASSWORD: {{ .Values.backend.postgres_admin_password | b64enc }}
{{- end }}